# Google Cloud project ID (required)
GOOGLE_CLOUD_PROJECT=your-gcp-project-id

# Gemini model to use for all agents (optional — defaults to gemini-2.5-flash)
MODEL=gemini-2.5-flash

# Google AI / Vertex AI API key (required if not using Application Default Credentials)
# GOOGLE_API_KEY=your-api-key-here
